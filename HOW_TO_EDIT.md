# ✏️ 가이드 추가 / 수정하는 법 (초보자 전용 가이드)

이 문서는 **HTML을 한 번도 만져본 적 없는 사람**을 위해 최대한 쉽게 쓴 설명서입니다.

---

## 왜 `guides-data.js` 파일만 만져야 하나요?

간단히 말해서:

- **`guides-data.js`** → 가이드 **내용(데이터)**만 들어있는 파일
- **`index.html`** → 사이트를 **실제로 작동**하게 만드는 모든 코드가 들어있는 파일

`index.html`을 실수로 조금만 잘못 고쳐도, 사이트가 완전히 안 열리거나 검색이 안 되는 등 큰 문제가 생길 수 있어요.

그래서 **가이드를 추가하거나 수정할 때는 무조건 `guides-data.js` 파일만 열어서 작업**하도록 설계했습니다.

`guides-data.js`는 그냥 "가이드 목록을 적어놓은 메모장"이라고 생각하면 됩니다. 여기서만 내용을 추가/수정하면 안전해요.

---

## 1. 가장 중요한 규칙

**가이드를 추가하거나 수정하고 싶을 때는 `guides-data.js` 파일만 만지면 됩니다.**

- `index.html` 파일은 **절대로** 손대지 마세요. (망칠 수 있음)
- `guides-data.js` 파일만 열어서 작업하세요.

---

## 2. 가이드 추가하는 기본 방법 (가장 자주 할 일)

### Step 1: `guides-data.js` 파일 열기

1. `starrail-guide` 폴더 안에서 `guides-data.js` 파일을 찾습니다.
2. 마우스 오른쪽 클릭 → **"연결 프로그램"** → **Visual Studio Code** 또는 **메모장**을 선택해서 엽니다.

### Step 2: 복사-붙여넣기로 추가하기

파일을 열면 맨 아래쪽에 이런 식으로 가이드들이 쭉 나열되어 있을 거예요:

```js
const guides = [
  { ... },
  { ... },
  { ... }
];
```

여기서 **마지막 가이드 뒤에 새로운 가이드를 추가**하면 됩니다.

### Step 3: 아래 템플릿을 복사해서 붙여넣기

파일 맨 아래에 있는 `];` 바로 **위**에 아래 내용을 붙여넣으세요.

```js
  {
    id: 99,                    // 숫자를 하나씩 올려주세요 (중요!)
    category: "파밍",          // 카테고리 (기본 / 파밍 / 캐릭터 / 세팅 / 미디어 중 하나)
    title: "가이드 제목",      // 제목 (이게 카드에 표시됨)
    desc: "간단한 설명 한 줄", // 카드에 보이는 짧은 설명
    content: `
      <h3>여기에 제목</h3>
      <p>여기에 자세한 내용</p>
      <ul>
        <li> bullet point</li>
      </ul>
    `
  },
```

### Step 4: 내용 채우기

- `id`: 기존에 있는 숫자 중 제일 큰 숫자 + 1 로 바꿔주세요. (예: 28번까지 있으면 29)
- `category`: 아래 중 하나만 정확히 써야 합니다:
  - `"기본"`
  - `"파밍"`
  - `"캐릭터"`
  - `"세팅"`
  - `"미디어"`
- `title`: 가이드 제목
- `desc`: 카드에 보일 짧은 설명 (1~2줄)
- `content`: 자세한 내용 (HTML로 작성)

### Step 5: 저장하고 확인

1. 파일을 저장합니다 (`Ctrl + S`)
2. `index.html`을 브라우저에서 새로고침 해봅니다.
3. 추가한 가이드가 잘 보이면 성공!

---

## 3. 자주 하는 실수 (주의!)

- 마지막 가이드 뒤에 **콤마( , )** 를 꼭 붙여야 합니다.
- `id` 숫자가 중복되면 안 됩니다.
- `category`는 반드시 위에 적힌 5개 단어 중 하나만 써야 합니다 (대소문자 구분).
- `content` 안에서 ` ` ` (백틱) 안에 HTML을 작성합니다.

---

## 4. 수정하는 법

기존 가이드를 수정하고 싶을 때는:

1. `guides-data.js` 열기
2. `Ctrl + F`로 수정하고 싶은 가이드 제목 검색
3. 해당 부분의 `title`, `desc`, `content` 등을 고치기
4. 저장 후 새로고침

---

## 5. 아직 모르겠으면?

지금은 **내용만 채우는 것**에 집중하세요.

UI(디자인)를 바꾸고 싶거나, 구조를 크게 바꾸고 싶으면 나중에 물어보세요.  
지금 단계에서는 `guides-data.js` 파일만 만지는 게 가장 안전합니다.

---

필요하면 이 문서에 더 자세한 예시를 추가해줄게!